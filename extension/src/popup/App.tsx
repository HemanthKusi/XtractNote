// Placeholder popup for File 1 — proves React renders inside the extension.
// The real detected-video card + action buttons arrive in File 4.
export default function App() {
    return (
      <main className="popup">
        <h1 className="popup__title">XtractNote</h1>
        <p className="popup__subtitle">Extension skeleton is running.</p>
      </main>
    );
  }